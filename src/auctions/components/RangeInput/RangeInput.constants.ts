/**
 * Max value that can be selected in {@link RangeInput} component
 */
export const maxInputValue = 20;
/**
 * Min value that can be selected in {@link RangeInput} component
 */
export const minInputValue = 1;
/**
 * Change of value when value is selected using arrow buttons in
 * {@link RangeInput} component
 */
export const step = 0.5;
